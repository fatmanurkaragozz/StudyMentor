# Supabase Veritabanı Bağlantı Bilgileri

# 1. Transaction Connection String (Supabase Dashboard > Settings > Database > Connection Pooling > Mode: Transaction - Port 6543)
# Örn: postgres://postgres.[your-project-id]:[your-password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true
DATABASE_URL="postgres://postgres.[PROJECT_REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true"

# 2. Direct Connection String (Supabase Dashboard > Settings > Database > Connection String > Mode: Session - Port 5432)
# Örn: postgres://postgres.[your-project-id]:[your-password]@aws-0-[region].pooler.supabase.com:5432/postgres
DIRECT_URL="postgres://postgres.[PROJECT_REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:5432/postgres"

# JWT Secret Anahtarı - MUTLAKA kendi rastgele değerinle değiştir.
# Üret: openssl rand -base64 32   (ya da: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))")
JWT_SECRET="<generate-a-random-32-byte-secret>"
PORT=5000

# NODE_ENV=production iken beklenmeyen hatalar istemciye generic mesajla döner (iç detay sızmaz)
NODE_ENV="development"

# ML servisi (FastAPI) adresi - yerelde varsayılan port 8000
ML_SERVICE_URL="http://localhost:8000"

# Gemini (Google) API - ders/uğraş bazlı AI yorumu özelliği için (opsiyonel)
# Boş bırakılırsa "AI Yorumu Al" butonu her zaman ücretsiz Kaptan şablon yorumuna düşer, sunucu yine çalışır
GEMINI_API_KEY=""

# E-posta doğrulama / şifremi unuttum kodlarını göndermek için Gmail SMTP
# GMAIL_APP_PASSWORD, normal Gmail şifren DEĞİL - Google Hesabında 2 Adımlı Doğrulama
# açıkken oluşturulan 16 haneli "Uygulama Şifresi" (myaccount.google.com/apppasswords)
GMAIL_USER="ornek@gmail.com"
GMAIL_APP_PASSWORD="xxxx xxxx xxxx xxxx"
